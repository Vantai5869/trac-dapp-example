# Local development (peer running locally)
# UPSTREAM_PROTOCOL=http
# UPSTREAM_HOST=127.0.0.1
# UPSTREAM_PORT=5001
# UPSTREAM_PREFIX=/v1

# Production / Remote peer via Cloudflare Tunnel
UPSTREAM_PROTOCOL=https
UPSTREAM_HOST=<your-tunnel>.trycloudflare.com
UPSTREAM_PORT=443
UPSTREAM_PREFIX=/v1

